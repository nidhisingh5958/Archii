name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.2.3'
      SERVERPOD_PASSWORD_database: 'yICJu7LEuyaCCVf7iGi4Wk-lMpYomLz4'
      SERVERPOD_PASSWORD_redis: 'Z3sdcd9d_MYbcBurFDn2CWdg95ubfzO0'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: archii_server_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: archii_server_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: archii_server_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: archii_server_server
        run: serverpod generate

      - name: Run tests
        working-directory: archii_server_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: archii_server_server
        run: docker compose down -v
